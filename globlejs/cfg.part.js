// --------------------------------------------
// cfg:公用配置对象. 方法小写开头,属性大写开头
// --------------------------------------------
let cfg = {};

// API 服务器地址,返回一个全路径地址.
cfg.apiUrl = (path) => { return 'http://127.0.0.1:8080' + path; };

ns.cfg = cfg;